# デフォルトは Git のテキスト自動判定に任せ、改行は LF に正規化する
* text=auto eol=lf

# シェル/設定系は常に LF (CRLF を持ち込ませない)
*.sh    text eol=lf
*.zsh   text eol=lf
*.bash  text eol=lf
.zshenv text eol=lf
.zshrc  text eol=lf

# バイナリは変換しない
*.png   binary
*.jpg   binary
*.gif   binary
*.zip   binary
