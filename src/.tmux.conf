# prefix keyをctrl-sに変更する

set -g prefix C-s
unbind C-b

set-option -g status on
set-option -g status-interval 1

set -g default-terminal "screen-256color"
set-option -g status-style bg="colour232"

# ペインボーダーの色を設定する
set -g pane-border-style fg=green

# アクティブなペインを目立たせる
set -g pane-active-border-style fg=blue

# message text
set-option -g message-style bg=colour245 #base02
set-option -g message-style fg=colour166 #orange

# pane number display
set-option -g display-panes-active-colour colour33 #blue
set-option -g display-panes-colour colour166 #orange

# clock
set-window-option -g clock-mode-colour colour64 #green

# キーストロークのディレイを減らす
set -sg escape-time 1

# ウィンドウのインデックスを1から始める
set -g base-index 1

# ペインのインデックスを1から始める
setw -g pane-base-index 1

# セッション終了時に番号を詰める
set-option -g renumber-windows on;

# 画面分割&新規セッション時にパスを引き継ぐ
bind c new-window -c "#{pane_current_path}"
# | でペインを縦に分割する
bind | split-window -hc "#{pane_current_path}"
# - でペインを横に分割する
bind - split-window -vc "#{pane_current_path}"

# 設定ファイルをリロードする
unbind r
bind r source-file ~/.tmux.conf \; display "Reloaded!"

# C-s 2回でtmux 内に C-s を送る
bind C-s send-prefix

# Vim のキーバインドでペインを移動する
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind -r C-h select-window -t :-
bind -r C-l select-window -t :+

# Vimのキーバインドでペインをリサイズする
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# コピーモードの設定
setw -g mouse on
setw -g mode-keys vi
unbind -T copy-mode MouseDragEnd1Pane
unbind -T copy-mode-vi MouseDragEnd1Pane
bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'select-pane -t=; copy-mode -e; send-keys -M'"
bind -n WheelDownPane select-pane -t= \; send-keys -M
bind -n C-WheelUpPane select-pane -t= \; copy-mode -e \; send-keys -M
bind -T copy-mode-vi    C-WheelUpPane   send-keys -X halfpage-up
bind -T copy-mode-vi    C-WheelDownPane send-keys -X halfpage-down
bind -T copy-mode-emacs C-WheelUpPane   send-keys -X halfpage-up
bind -T copy-mode-emacs C-WheelDownPane send-keys -X halfpage-down
unbind -T copy-mode-vi Enter

# クリップボード連携コマンドを OS ごとに @copy_command へ集約する
# (Enter / 左クリックの両方から参照するため、ここで一元定義)
if-shell "uname | grep -q Darwin" \
    'set -g @copy_command "pbcopy"'
# Linux (xsel)
if-shell 'type xsel' \
    'set -g @copy_command "xsel -ib"'
# WSL (clip.exe)
if-shell 'type clip.exe' \
    'set -g @copy_command "tmux save-buffer - | /mnt/c/Windows/System32/clip.exe"'

# Enter で選択範囲を確定してコピー
bind-key -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "#{@copy_command}"

# 左クリックでも Enter と同じくコピー確定する。
# ドラッグ選択後 (MouseDragEnd は unbind 済みで選択が残る) に左クリックすると、
# 選択がある場合はコピーして copy-mode を抜け、無い場合は選択解除にフォールバックする。
bind-key -T copy-mode-vi MouseDown1Pane if-shell -F "#{selection_present}" \
    'send-keys -X copy-pipe-and-cancel "#{@copy_command}"' \
    'send-keys -X clear-selection'

# キーボードからの vi 風コピー操作 (マウスと同じ @copy_command を使う)
bind-key -T copy-mode-vi v   send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y   send-keys -X copy-pipe-and-cancel "#{@copy_command}"

###### StatusLine #######

#set -g status-left-length 20
#set -g status-left '#[fg=colour232,bg=colour255,bold] Session:#S #[fg=colour255,bg=colour232,nobold]'

set -g status-right-length 50
set -g status-right '#[fg=colour238,nobold] #[fg=colour248,bg=colour238,nobold] %Y-%m-%d |#[fg=colour252,bg=colour238,bold] %H:%M:%S #[fg=colour233,bg=colour252,bold] #H '

setw -g window-status-format "#[fg=colour250,bg=colour232,nobold] #I:#{=20:window_name} "
setw -g window-status-current-format "#[fg=colour232,bg=colour104,nobold] #[fg=colour255,bg=colour104,bold] #I:#{=20:window_name} #[fg=colour104,bg=colour232,nobold] "

###########################

###### Plugins (TPM) ######
# tmux plugin manager と、セッション永続化プラグインを宣言する。
# 初回のみ `prefix + I` でインストール (下の自動 clone で TPM 自体は導入される)。
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'   # 手動でセッション保存/復元
set -g @plugin 'tmux-plugins/tmux-continuum'   # 自動保存 + 起動時に自動復元

# prefix (C-s) と衝突しないよう resurrect のキーを変更
set -g @resurrect-save 'S'                      # prefix + S で保存
set -g @resurrect-restore 'R'                   # prefix + R で復元
set -g @resurrect-capture-pane-contents 'on'    # ペインの表示内容も復元
set -g @resurrect-strategy-vim 'session'        # vim のセッションを復元

set -g @continuum-restore 'on'                  # tmux 起動時に自動復元
set -g @continuum-save-interval '15'            # 15 分ごとに自動保存

# TPM が未導入なら自動で clone する
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/tpm'"

# 【重要】この行は必ずファイル末尾に置くこと
run '~/.tmux/plugins/tpm/tpm'
